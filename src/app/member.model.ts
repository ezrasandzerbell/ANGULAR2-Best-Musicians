export class Member {
  constructor (public name: string, public awards: string, public details: string, public image: string, public yearsActive: string, public country: string, public genre: string, public video: string) { }
}
